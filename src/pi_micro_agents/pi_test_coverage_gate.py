"from __future__ import annotations\n\nimport re\n\nfrom pydantic import BaseModel, Field\n\n\nclass CoverageInput(BaseModel):\n    coverage_report: str = Field(..., description=\"String contents of the test coverage report\")\n    min_threshold: float = Field(..., description=\"Minimum acceptable coverage percentage (0.0 to 100.0)\")\n\n\nclass CoverageOutput(BaseModel):\n    meets_threshold: bool = Field(..., description=\"Indicates if coverage meets or exceeds threshold\")\n    actual_coverage: float = Field(..., description=\"Identified coverage percentage value\")\n    status: str = Field(..., description=\"Coverage enforcement status\")\n\n\nclass PiTestCoverageGate:\n    \"\"\"Parses coverage report and checks against a minimum percentage threshold.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiTestCoverageGate\"\n\n    def check_coverage(self, input_envelope: CoverageInput) -> CoverageOutput:\n        report = input_envelope.coverage_report\n        threshold = input_envelope.min_threshold\n\n        # Look for typical coverage percentages:\n        # e.g., \"TOTAL         54     10    81%\"\n        # \"Coverage: 92.5%\"\n        # \"<line-rate=\"0.95\">\"\n        # \"Total Coverage: 88.42%\"\n        patterns = [\n            r\"TOTAL\\s+\\d+\\s+\\d+\\s+(\\d+(?:\\.\\d+)?)%\",\n            r\"Coverage:\\s*(\\d+(?:\\.\\d+)?)%\",\n            r\"Total Coverage:\\s*(\\d+(?:\\.\\d+)?)%\",\n            r\"line-rate=\\\"(0\\.\\d+|1(?:\\.0+)?)\\\"\",\n        ]\n\n        actual_cov = 0.0\n        found = False\n\n        for pattern in patterns:\n            match = re.search(pattern, report, re.IGNORECASE)\n            if match:\n                val = float(match.group(1))\n                # If matched a line-rate decimal (e.g. 0.95), convert to percentage\n                if \"line-rate\" in pattern:\n                    val = val * 100.0\n                actual_cov = val\n                found = True\n                break\n\n        # Fallback: find any standalone percentage if n
<truncated 553 bytes>