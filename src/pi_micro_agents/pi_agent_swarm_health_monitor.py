"from __future__ import annotations\n\nfrom typing import List, Dict, Any\nfrom pydantic import BaseModel, Field\n\n\nclass SwarmInput(BaseModel):\n    agent_statuses: List[Dict[str, Any]] = Field(..., description=\"Status reports of all active agents in the swarm\")\n\n\nclass SwarmOutput(BaseModel):\n    healthy: bool = Field(..., description=\"Overall health of the agent swarm\")\n    failing_agents: List[str] = Field(default_factory=list, description=\"List of failing agent names\")\n    recovery_recommendations: List[str] = Field(default_factory=list, description=\"List of recovery steps\")\n    status: str = Field(..., description=\"Swarm health status indicator\")\n\n\nclass PiAgentSwarmHealthMonitor:\n    \"\"\"Holistic health check for an entire running swarm of Pi agents.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiAgentSwarmHealthMonitor\"\n\n    def monitor_swarm(self, input_envelope: SwarmInput) -> SwarmOutput:\n        statuses = input_envelope.agent_statuses\n\n        failing = []\n        recommendations = []\n\n        for status in statuses:\n            name = status.get(\"agent_name\", \"unknown\")\n            state = status.get(\"status\", \"ACTIVE\")\n            latency = status.get(\"latency_ms\", 0.0)\n\n            # Check standard failure states\n            if state in [\"ERROR\", \"TIMEOUT\", \"FAILED\", \"BLOCKED\"]:\n                failing.append(name)\n                recommendations.append(f\"Restart agent '{name}' or switch to localized fallback runner.\")\n            elif latency > 5000.0:\n                # Latency warning\n                failing.append(f\"{name} (Latency High)\")\n                recommendations.append(f\"Optimize memory limits or scale resources for '{name}'.\")\n\n        healthy = len(failing) == 0\n        status = \"SWARM_HEALTHY\" if healthy else \"SWARM_DEGRADED\"\n\n        if not healthy:\n            recommendations.append(\"Trigger automated self-healing coordination rules across broken components.\")\n\n        retu
<truncated 178 bytes>