"from __future__ import annotations\n\nimport re\nfrom typing import Set\nfrom pydantic import BaseModel, Field\n\n\nclass ReflectionInput(BaseModel):\n    agent_output: str = Field(..., description=\"The generated output from the agent\")\n    original_task: str = Field(..., description=\"The original task description or prompt\")\n\n\nclass ReflectionOutput(BaseModel):\n    is_coherent: bool = Field(..., description=\"Indicates if the output matches the task requirements\")\n    drift_score: float = Field(..., description=\"Calculated semantic drift score (0.0 to 100.0)\")\n    suggested_fix: str = Field(..., description=\"Actionable deterministic correction suggestion\")\n    status: str = Field(..., description=\"Operational validation status\")\n\n\nclass PiAgentSelfReflectionChecker:\n    \"\"\"Compares an agent's output against its original task using deterministic semantic overlap + checklist rules.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiAgentSelfReflectionChecker\"\n\n    def _tokenize(self, text: str) -> Set[str]:\n        words = re.findall(r\"\\b[a-zA-Z0-9_-]+\\b\", text.lower())\n        return set(words)\n\n    def check_reflection(self, input_envelope: ReflectionInput) -> ReflectionOutput:\n        output_tokens = self._tokenize(input_envelope.agent_output)\n        task_tokens = self._tokenize(input_envelope.original_task)\n\n        if not task_tokens:\n            return ReflectionOutput(\n                is_coherent=True,\n                drift_score=0.0,\n                suggested_fix=\"None\",\n                status=\"PASSED\"\n            )\n\n        intersection = task_tokens.intersection(output_tokens)\n        overlap_ratio = len(intersection) / len(task_tokens)\n        drift_score = max(0.0, min(100.0, (1.0 - overlap_ratio) * 100.0))\n\n        # Check for absolute negation keywords in output if present in task (contradiction detector)\n        negation_words = {\"not\", \"never\", \"cannot\", \"fail\", \"unable\", \"error\"}\n        negation_in_outpu
<truncated 930 bytes>