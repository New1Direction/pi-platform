"from __future__ import annotations\n\nimport ast\n\nfrom pydantic import BaseModel, Field\n\n\nclass CodeInput(BaseModel):\n    content: str = Field(..., description=\"String contents of python source code\")\n    language: str = Field(default=\"python\", description=\"Programming language context\")\n\n\nclass ExceptionOutput(BaseModel):\n    try_blocks: int = Field(..., description=\"Total count of try blocks identified\")\n    bare_except: bool = Field(..., description=\"True if any bare except: block was found\")\n    status: str = Field(..., description=\"Exception block analysis status\")\n\n\nclass PiExceptionHandlingSentry:\n    \"\"\"Counts try blocks and flags dangerous bare except: clauses.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiExceptionHandlingSentry\"\n\n    def audit_exception_handling(self, input_envelope: CodeInput) -> ExceptionOutput:\n        content = input_envelope.content\n\n        try_blocks = 0\n        bare_except = False\n\n        try:\n            tree = ast.parse(content)\n            for node in ast.walk(tree):\n                if isinstance(node, ast.Try):\n                    try_blocks += 1\n                    for handler in node.handlers:\n                        # handler.type is None for a bare except:\n                        if handler.type is None:\n                            bare_except = True\n\n        except Exception:\n            # Fallback when parsing fails: line-by-line checks\n            lines = content.splitlines()\n            for line in lines:\n                cleaned = line.split(\"#\")[0].strip()\n                if cleaned.startswith(\"try:\"):\n                    try_blocks += 1\n                if cleaned.startswith(\"except:\") or cleaned.startswith(\"except :\"):\n                    bare_except = True\n\n        status = \"BARE_EXCEPT_DETECTED\" if bare_except else \"EXCEPTION_HANDLING_OK\"\n\n        return ExceptionOutput(\n            try_blocks=try_blocks,\n            bare_except=bare_except,\n            stat
<truncated 23 bytes>