"from __future__ import annotations\n\nimport difflib\nfrom typing import List, Dict, Any\nfrom pydantic import BaseModel, Field\n\n\nclass ConsistencyInput(BaseModel):\n    outputs: List[str] = Field(..., description=\"List of outputs to compare for consistency verification\")\n    schema: Dict[str, Any] = Field(default_factory=dict, description=\"Structural validation schema checklist\")\n\n\nclass ConsistencyOutput(BaseModel):\n    is_consistent: bool = Field(..., description=\"Indicates if all outputs meet the consistency threshold\")\n    variance_score: float = Field(..., description=\"Calculated consistency variance percentage (0.0 to 100.0)\")\n    status: str = Field(..., description=\"Consistency validation status\")\n\n\nclass PiOutputConsistencyGate:\n    \"\"\"Ensures multiple agent runs on the same input produce identical structured results.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiOutputConsistencyGate\"\n\n    def verify_consistency(self, input_envelope: ConsistencyInput) -> ConsistencyOutput:\n        runs = input_envelope.outputs\n        \n        if not runs:\n            return ConsistencyOutput(\n                is_consistent=True,\n                variance_score=0.0,\n                status=\"NO_RUNS_SUBMITTED\"\n            )\n\n        if len(runs) == 1:\n            return ConsistencyOutput(\n                is_consistent=True,\n                variance_score=0.0,\n                status=\"SINGLE_RUN_CONSISTENT\"\n            )\n\n        total_comparisons = 0\n        sum_similarity = 0.0\n\n        # Perform pairwise comparisons\n        for i in range(len(runs)):\n            for j in range(i + 1, len(runs)):\n                s = difflib.SequenceMatcher(None, runs[i], runs[j])\n                sum_similarity += s.ratio()\n                total_comparisons += 1\n\n        avg_similarity = sum_similarity / total_comparisons\n        variance_score = (1.0 - avg_similarity) * 100.0\n\n        # Consistent if average similarity is greater than 90%\n        is
<truncated 282 bytes>