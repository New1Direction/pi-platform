"from __future__ import annotations\n\nimport re\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\n\nclass ResourceInput(BaseModel):\n    content: str = Field(..., description=\"String contents of the source file\")\n    language: str = Field(default=\"python\", description=\"Programming language of the file\")\n\n\nclass ResourceOutput(BaseModel):\n    open_resources: List[str] = Field(default_factory=list, description=\"List of unclosed or risky resource statements identified\")\n    status: str = Field(..., description=\"Resource leak status\")\n\n\nclass PiResourceLeakScanner:\n    \"\"\"Detects file/socket/database handles opened without matching close statements.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiResourceLeakScanner\"\n\n    def scan_resource_leaks(self, input_envelope: ResourceInput) -> ResourceOutput:\n        content = input_envelope.content\n        lines = content.splitlines()\n\n        risky_statements = []\n\n        # Python-specific pattern: finding file opens that are not using \"with\"\n        # and do not have an explicit close statement on subsequent lines\n        has_close = any(\".close(\" in line for line in lines)\n\n        for idx, line in enumerate(lines, start=1):\n            cleaned = line.split(\"#\")[0].strip()\n\n            # e.g., f = open(...) or conn = sqlite3.connect(...)\n            # Skip if it is a context manager block\n            if \"with \" in cleaned:\n                continue\n\n            open_match = re.search(r\"(\\w+)\\s*=\\s*(?:open|connect|socket)\\s*\\(\", cleaned)\n            if open_match:\n                var_name = open_match.group(1)\n                # Look for a closing line with var_name.close()\n                close_found = False\n                for rest_line in lines[idx:]:\n                    if f\"{var_name}.close(\" in rest_line:\n                        close_found = True\n                        break\n                if not close_found:\n                    risky_statements.append
<truncated 295 bytes>