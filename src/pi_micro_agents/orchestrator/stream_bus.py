"\"\"\"\nstream_bus.py — Typed per-step streaming event bus for Pi chain execution.\n\nProvides:\n  - ChainEventType: typed enum of all possible chain lifecycle events\n  - ChainEvent: Pydantic model for a single event emission\n  - ChainStreamBus: thread-safe in-process queue-backed event bus\n\"\"\"\nfrom __future__ import annotations\n\nimport queue\nimport time\nimport uuid\nfrom enum import Enum\nfrom typing import Generator, Iterator, Optional\n\nfrom pydantic import BaseModel, Field\n\n\nclass ChainEventType(str, Enum):\n    CHAIN_STARTED = \"CHAIN_STARTED\"\n    STEP_STARTED = \"STEP_STARTED\"\n    STEP_COMPLETED = \"STEP_COMPLETED\"\n    STEP_FAILED = \"STEP_FAILED\"\n    REWIND_TRIGGERED = \"REWIND_TRIGGERED\"\n    CACHE_HIT = \"CACHE_HIT\"\n    SPECULATIVE_BRANCH = \"SPECULATIVE_BRANCH\"\n    RETRY_ATTEMPT = \"RETRY_ATTEMPT\"\n    CHECKPOINT_SAVED = \"CHECKPOINT_SAVED\"\n    CHECKPOINT_RESUMED = \"CHECKPOINT_RESUMED\"\n    CHAIN_COMPLETED = \"CHAIN_COMPLETED\"\n    CHAIN_FAILED = \"CHAIN_FAILED\"\n\n\nclass ChainEvent(BaseModel):\n    event_id: str = Field(default_factory=lambda: \"evt_\" + uuid.uuid4().hex[:8])\n    event_type: ChainEventType\n    chain_id: str\n    step_index: Optional[int] = None\n    agent_name: Optional[str] = None\n    risk_score: Optional[float] = None\n    latency_ms: Optional[float] = None\n    success: Optional[bool] = None\n    summary: str = \"\"\n    metadata: dict = Field(default_factory=dict)\n    timestamp: float = Field(default_factory=time.time)\n\n    def as_dict(self) -> dict:\n        return self.model_dump()\n\n    def __repr__(self) -> str:\n        parts = [f\"[{self.event_type.value}]\", f\"chain={self.chain_id}\"]\n        if self.agent_name:\n            parts.append(f\"agent={self.agent_name}\")\n        if self.step_index is not None:\n            parts.append(f\"step={self.step_index}\")\n        if self.risk_score is not None:\n            parts.append(f\"risk={self.risk_score:.1f}\")\n        if self.latency_ms is not None:\n            parts.append(f\
<truncated 3117 bytes>