"from __future__ import annotations\n\nfrom typing import List, Dict, Any\nfrom pydantic import BaseModel, Field\n\n\nclass CoordInput(BaseModel):\n    agent_outputs: List[Dict[str, Any]] = Field(..., description=\"List of outputs from executed agents in the workflow\")\n    expected_flow: Dict[str, Any] = Field(..., description=\"Expected coordination flow representation (DAG or sequential mappings)\")\n\n\nclass CoordOutput(BaseModel):\n    is_valid: bool = Field(..., description=\"Indicates if the workflow coordination was synchronized correctly\")\n    desync_points: List[str] = Field(default_factory=list, description=\"List of desynchronization points or missing dependencies\")\n    status: str = Field(..., description=\"Flow coordination status description\")\n\n\nclass PiMultiAgentCoordinationValidator:\n    \"\"\"Validates handoffs between multiple Pi agents against a predefined workflow graph.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiMultiAgentCoordinationValidator\"\n\n    def validate_coordination(self, input_envelope: CoordInput) -> CoordOutput:\n        outputs = input_envelope.agent_outputs\n        expected = input_envelope.expected_flow\n        \n        desyncs = []\n        \n        # 1. Verify sequence order if sequential dependencies are described in expected flow\n        # e.g., expected = {\"sequence\": [\"PiGitSecScanner\", \"PiSelfHealingPatchAgent\"]}\n        sequence = expected.get(\"sequence\", [])\n        executed_agents = [out.get(\"agent_name\", \"\") for out in outputs if isinstance(out, dict)]\n        \n        for idx, expected_agent in enumerate(sequence):\n            if expected_agent not in executed_agents:\n                desyncs.append(f\"Missing Step: Agent '{expected_agent}' did not execute in the flow.\")\n            else:\n                exec_idx = executed_agents.index(expected_agent)\n                if exec_idx > 0 and idx > 0:\n                    prev_expected = sequence[idx - 1]\n                    prev_actual = executed_age
<truncated 1434 bytes>