"from __future__ import annotations\n\nimport os\nimport re\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\n\n# 1. Strict-mode config resolver\ndef is_strict_mode() -> bool:\n    env_val = os.getenv(\"PI_MIME_STRICT_MODE\")\n    if env_val is not None:\n        return env_val.lower() == \"true\"\n    return True\n\n\n# File extension → canonical MIME type mapping\n_EXT_TO_MIME: dict[str, str] = {\n    \".html\": \"text/html\",\n    \".htm\": \"text/html\",\n    \".css\": \"text/css\",\n    \".js\": \"application/javascript\",\n    \".mjs\": \"application/javascript\",\n    \".json\": \"application/json\",\n    \".xml\": \"application/xml\",\n    \".pdf\": \"application/pdf\",\n    \".png\": \"image/png\",\n    \".jpg\": \"image/jpeg\",\n    \".jpeg\": \"image/jpeg\",\n    \".gif\": \"image/gif\",\n    \".webp\": \"image/webp\",\n    \".svg\": \"image/svg+xml\",\n    \".ico\": \"image/x-icon\",\n    \".mp4\": \"video/mp4\",\n    \".webm\": \"video/webm\",\n    \".mp3\": \"audio/mpeg\",\n    \".wav\": \"audio/wav\",\n    \".ogg\": \"audio/ogg\",\n    \".zip\": \"application/zip\",\n    \".gz\": \"application/gzip\",\n    \".tar\": \"application/x-tar\",\n    \".txt\": \"text/plain\",\n    \".csv\": \"text/csv\",\n    \".wasm\": \"application/wasm\",\n    \".ttf\": \"font/ttf\",\n    \".woff\": \"font/woff\",\n    \".woff2\": \"font/woff2\",\n}\n\n# Dangerous MIME mismatches (extension → claimed_mime that is wrong AND risky)\n_DANGEROUS_MISMATCHES = {\n    (\"html\", \"application/octet-stream\"),\n    (\"js\", \"text/plain\"),\n    (\"svg\", \"text/html\"),  # SVG as HTML can execute script\n}\n\n\ndef check_mime_consistency(\n    file_path: str,\n    declared_mime: str,\n) -> tuple[List[str], float]:\n    \"\"\"Validates file extension ↔ declared MIME type consistency.\"\"\"\n    findings: List[str] = []\n    risk = 0.0\n\n    ext_match = re.search(r\"(\\.[a-zA-Z0-9]+)$\", file_path.lower())\n    if not ext_match:\n        return findings, risk\n\n    ext = ext_match.group(1).lower()\n  
<truncated 3415 bytes>