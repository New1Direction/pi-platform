"from __future__ import annotations\n\nimport ast\n\nfrom pydantic import BaseModel, Field\n\n\nclass DocstringInput(BaseModel):\n    content: str = Field(..., description=\"String contents of the source code\")\n    language: str = Field(default=\"python\", description=\"Programming language of the source file\")\n\n\nclass DocstringOutput(BaseModel):\n    has_docstring: bool = Field(..., description=\"Indicates if evaluated functions have docstrings\")\n    complete: bool = Field(..., description=\"True if 100% of public functions have non-empty docstrings\")\n    status: str = Field(..., description=\"Docstring validation status\")\n\n\nclass PiFunctionDocstringValidator:\n    \"\"\"Verifies every public function has a non-empty docstring.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiFunctionDocstringValidator\"\n\n    def validate_docstrings(self, input_envelope: DocstringInput) -> DocstringOutput:\n        content = input_envelope.content\n        language = input_envelope.language.lower()\n\n        # If language is python, we can parse AST for highly precise deterministic checks.\n        # Fallback to simple regex/line matching for other languages.\n        if language == \"python\":\n            try:\n                tree = ast.parse(content)\n                public_functions = []\n                missing_docstrings = []\n\n                for node in ast.walk(tree):\n                    if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):\n                        # Skip private/internal methods starting with \"_\" except __init__ or similar if desired.\n                        # Standard convention: skip if starts with \"_\" and not starts with \"__\"\n                        name = node.name\n                        if name.startswith(\"_\") and not name.startswith(\"__\"):\n                            continue\n\n                        public_functions.append(name)\n                        docstring = ast.get_docstring(node)\n                        if not docstrin
<truncated 945 bytes>