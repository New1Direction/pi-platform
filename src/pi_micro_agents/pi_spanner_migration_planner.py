"from __future__ import annotations\n\nimport logging\nimport re\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\nfrom pi_micro_agents.gcp_client import SPANNER_AVAILABLE\n\nlogger = logging.getLogger(\"pi_spanner_migration_planner\")\n\n\nclass SpannerMigrationInput(BaseModel):\n    source_schema_ddl: str = Field(..., description=\"PostgreSQL or MySQL source schema definition DDL\")\n    target_instance_id: str = Field(default=\"\", description=\"Optional Spanner instance ID to verify\")\n    dry_run: bool = Field(default=True, description=\"When True, translates DDL without validating against GCP APIs\")\n\n\nclass SpannerMigrationOutput(BaseModel):\n    ddl_generated: bool = Field(..., description=\"True if source schema was parsed and translated successfully\")\n    spanner_ddl: str = Field(..., description=\"The translated Spanner DDL schema\")\n    migration_difficulty: str = Field(..., description=\"Migration difficulty tier: LOW, MEDIUM, or HIGH\")\n    estimated_migration_hours: float = Field(..., description=\"Estimated hours to execute migration\")\n    issues: List[str] = Field(default_factory=list, description=\"Warnings, constraints, or issues flagged during translation\")\n    dry_run_executed: bool = Field(..., description=\"True if dry_run logic was utilized\")\n\n\nclass PiSpannerMigrationPlanner:\n    \"\"\"Translates MySQL/PostgreSQL schema DDL definitions to Cloud Spanner DDL structures, verifying type compatibility, primary keys, and interleaving options.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiSpannerMigrationPlanner\"\n\n    def execute(self, input_envelope: SpannerMigrationInput) -> SpannerMigrationOutput:\n        \"\"\"Translate source DDL to Spanner DDL schema format.\"\"\"\n        source_ddl = input_envelope.source_schema_ddl\n        target_inst = input_envelope.target_instance_id.strip()\n        dry_run = input_envelope.dry_run\n\n        issues: List[str] = []\n        translated_lines: List[str] = []\n        difficulty = \"LOW
<truncated 3401 bytes>