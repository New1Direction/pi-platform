"from __future__ import annotations\n\nimport re\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\n\nclass ErrorInput(BaseModel):\n    content: str = Field(..., description=\"String contents of the source file\")\n\n\nclass ErrorOutput(BaseModel):\n    uses_templates: bool = Field(..., description=\"True if error messages utilize central templates\")\n    raw_strings: List[str] = Field(default_factory=list, description=\"List of raw inline error string violations\")\n    status: str = Field(..., description=\"Error structure check status\")\n\n\nclass PiErrorMessageTemplateChecker:\n    \"\"\"Verifies error messages use centralized templates instead of inline strings.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiErrorMessageTemplateChecker\"\n\n    def check_error_templates(self, input_envelope: ErrorInput) -> ErrorOutput:\n        content = input_envelope.content\n        lines = content.splitlines()\n\n        violations = []\n\n        # Find raw exception throws raising with simple inline string arguments:\n        # e.g., raise ValueError(\"Invalid item\"), throw new Error(\"bad\")\n        patterns = [\n            r\"\\braise\\s+\\w+Error\\s*\\(\\s*['\\\"]([^'\\\"]+)['\\\"]\",\n            r\"\\bthrow\\s+new\\s+Error\\s*\\(\\s*['\\\"]([^'\\\"]+)['\\\"]\",\n        ]\n\n        for idx, line in enumerate(lines, start=1):\n            cleaned = line.split(\"#\")[0].strip()\n\n            for pattern in patterns:\n                match = re.search(pattern, cleaned)\n                if match:\n                    raw_str = match.group(1)\n                    # We expect error templates or centralized identifiers:\n                    # e.g. ErrorCode.XXX or string starting with central patterns.\n                    # Simple rule: if string contains spaces and is all lowercase or standard words,\n                    # it's likely a raw inline error message instead of an error identifier.\n                    if \" \" in raw_str or raw_str.islower():\n              
<truncated 410 bytes>