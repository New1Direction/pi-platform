"from __future__ import annotations\n\nimport json\nimport os\nfrom typing import Any, Dict, List\n\nfrom pydantic import BaseModel, Field\n\n\n# 1. Strict-mode config resolver\ndef is_strict_mode() -> bool:\n    env_val = os.getenv(\"PI_JSON_SCHEMA_STRICT_MODE\")\n    if env_val is not None:\n        return env_val.lower() == \"true\"\n    return True\n\n\ndef check_json_schema_fields(\n    json_text: str,\n    required_fields: List[str],\n    forbidden_fields: List[str],\n) -> tuple[List[str], List[str], List[str], float]:\n    \"\"\"\n    Validates a JSON string against required/forbidden field constraints.\n    Returns (missing_required, extra_forbidden, parse_errors, risk_score).\n    \"\"\"\n    parse_errors: List[str] = []\n    missing_required: List[str] = []\n    extra_forbidden: List[str] = []\n    risk = 0.0\n\n    if not json_text.strip():\n        parse_errors.append(\"Empty JSON payload provided.\")\n        return missing_required, extra_forbidden, parse_errors, 30.0\n\n    try:\n        data: Any = json.loads(json_text)\n    except json.JSONDecodeError as exc:\n        parse_errors.append(f\"Invalid JSON — parse error at position {exc.pos}: {exc.msg}\")\n        return missing_required, extra_forbidden, parse_errors, 60.0\n\n    if not isinstance(data, dict):\n        parse_errors.append(f\"Expected a JSON object (dict) at root; got {type(data).__name__}.\")\n        return missing_required, extra_forbidden, parse_errors, 40.0\n\n    top_level_keys: set[str] = set(data.keys())\n\n    for field in required_fields:\n        if field not in top_level_keys:\n            missing_required.append(field)\n            risk = max(risk, 70.0)\n\n    for field in forbidden_fields:\n        if field in top_level_keys:\n            extra_forbidden.append(field)\n            risk = max(risk, 80.0)\n\n    return missing_required, extra_forbidden, parse_errors, risk\n\n\n# 3. Pydantic Schemas\nclass JsonSchemaInput(BaseModel):\n    file_path: str = Field(..., description=\"Source path or label for the JSON being
<truncated 2380 bytes>