"from __future__ import annotations\n\nimport os\nimport re\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\n\n# 1. Strict-mode config resolver\ndef is_strict_mode() -> bool:\n    env_val = os.getenv(\"PI_LICENSE_HEADER_STRICT_MODE\")\n    if env_val is not None:\n        return env_val.lower() == \"true\"\n    return True\n\n\n# Default required license header keywords (any one of these satisfies the check)\n_DEFAULT_LICENSE_KEYWORDS = [\n    \"copyright\",\n    \"license\",\n    \"spdx-license-identifier\",\n    \"apache license\",\n    \"mit license\",\n    \"gnu general public\",\n    \"mozilla public\",\n    \"bsd license\",\n    \"proprietary\",\n    \"all rights reserved\",\n]\n\n# Source file extensions that require license headers\n_LICENSED_EXTENSIONS = {\n    \".py\", \".js\", \".ts\", \".jsx\", \".tsx\", \".go\", \".rs\", \".java\", \".c\", \".cpp\",\n    \".h\", \".hpp\", \".cs\", \".rb\", \".scala\", \".kt\", \".swift\", \".m\", \".sol\",\n}\n\n\ndef check_license_header(\n    file_path: str,\n    content: str,\n    required_keywords: List[str],\n    check_lines: int,\n) -> tuple[bool, List[str], float]:\n    \"\"\"\n    Checks whether a source file's first `check_lines` lines contain a license/copyright header.\n    Returns (has_header, findings, risk).\n    \"\"\"\n    findings: List[str] = []\n    risk = 0.0\n\n    # Determine if this file type requires a license header\n    ext = os.path.splitext(file_path)[1].lower()\n    if ext not in _LICENSED_EXTENSIONS:\n        return True, findings, risk\n\n    if not content.strip():\n        return True, findings, risk\n\n    # Inspect first `check_lines` lines\n    header_region = \"\\n\".join(content.splitlines()[:check_lines]).lower()\n\n    keywords = required_keywords if required_keywords else _DEFAULT_LICENSE_KEYWORDS\n    has_header = any(kw.lower() in header_region for kw in keywords)\n\n    if not has_header:\n        findings.append(\n            f\"Missing license/copyright header in '{file_path}' — \"\n            f\"no
<truncated 2419 bytes>