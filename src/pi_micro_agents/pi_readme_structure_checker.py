"from __future__ import annotations\n\nimport re\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\n\nclass ReadmeInput(BaseModel):\n    content: str = Field(..., description=\"Markdown contents of the README file\")\n\n\nclass ReadmeOutput(BaseModel):\n    has_sections: bool = Field(..., description=\"Indicates if standard documentation sections are present\")\n    missing_sections: List[str] = Field(default_factory=list, description=\"List of missing standard sections\")\n    status: str = Field(..., description=\"README structure validation status\")\n\n\nclass PiReadmeStructureChecker:\n    \"\"\"Checks for standard README sections (Usage, Install, License, etc.).\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiReadmeStructureChecker\"\n\n    def check_readme_structure(self, input_envelope: ReadmeInput) -> ReadmeOutput:\n        content = input_envelope.content\n\n        # Standard documentation headings\n        standard_headings = [\n            \"usage\",\n            \"install\",\n            \"installation\",\n            \"license\",\n            \"contributing\",\n            \"features\",\n            \"getting started\",\n            \"quick start\",\n        ]\n\n        missing = []\n        # Find all headings: `#`, `##`, `###`, etc.\n        headings = re.findall(r\"^#+\\s+(.+)$\", content, re.MULTILINE)\n        headings_lower = [h.strip().lower() for h in headings]\n\n        # Check standard sections\n        # We will require a minimum subset: Usage, Installation/Install, License.\n        # Let's see which of these standard ones are missing.\n        required_subset = [\"usage\", \"license\"]\n        has_install = False\n\n        for heading in headings_lower:\n            if \"install\" in heading or \"installation\" in heading:\n                has_install = True\n\n        for req in required_subset:\n            found = False\n            for heading in headings_lower:\n                if req in heading:\n                    found = True\n    
<truncated 466 bytes>