"from __future__ import annotations\n\nimport base64\nimport json\nimport os\nimport time\nfrom typing import List, Optional\n\nfrom pydantic import BaseModel, Field\n\n\n# 1. Strict-mode config resolver\ndef is_strict_mode() -> bool:\n    env_val = os.getenv(\"PI_JWT_STRICT_MODE\")\n    if env_val is not None:\n        return env_val.lower() == \"true\"\n    return True\n\n\ndef _b64_decode_segment(segment: str) -> Optional[dict]:\n    \"\"\"Decodes a base64url JWT segment to a Python dict without verification.\"\"\"\n    # Add padding\n    padded = segment + \"=\" * (-len(segment) % 4)\n    # Replace URL-safe characters\n    padded = padded.replace(\"-\", \"+\").replace(\"_\", \"/\")\n    try:\n        decoded = base64.b64decode(padded).decode(\"utf-8\", errors=\"replace\")\n        return json.loads(decoded)\n    except Exception:\n        return None\n\n\ndef audit_jwt(token: str) -> tuple[List[str], float]:\n    \"\"\"\n    Decodes a JWT (without verification) and checks for:\n    - Missing required claims (exp, iat, nbf)\n    - Expired tokens (exp < now)\n    - Not-yet-valid tokens (nbf > now)\n    - 'none' algorithm in header (critical vulnerability)\n    - Excessively long expiry (> 24 hours)\n    - Missing subject/issuer claims\n    \"\"\"\n    findings: List[str] = []\n    risk = 0.0\n\n    # JWT must be 3 dot-separated segments\n    parts = token.strip().split(\".\")\n    if len(parts) != 3:\n        findings.append(f\"INVALID: JWT does not have 3 segments (found {len(parts)}) — malformed token.\")\n        return findings, 60.0\n\n    header = _b64_decode_segment(parts[0])\n    payload = _b64_decode_segment(parts[1])\n\n    if header is None:\n        findings.append(\"INVALID: JWT header segment could not be decoded.\")\n        return findings, 60.0\n\n    if payload is None:\n        findings.append(\"INVALID: JWT payload segment could not be decoded.\")\n        return findings, 60.0\n\n    now = int(time.time())\n\n    # ---- Header checks ----\n    alg = str(header.get(\"alg\", \"\")).lower()\
<truncated 4498 bytes>