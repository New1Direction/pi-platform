"from __future__ import annotations\n\nimport logging\nimport re\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\nfrom pi_micro_agents.gcp_client import TRACE_AVAILABLE\n\nlogger = logging.getLogger(\"pi_cloud_trace_span_emitter\")\n\n_TRACE_ID_PATTERN = re.compile(r\"^[a-fA-F0-9]{32}$\")\n\n\nclass CloudTraceSpanInput(BaseModel):\n    trace_id: str = Field(..., description=\"Unique 32-char hex string representing the trace context\")\n    span_name: str = Field(..., description=\"Logical name of the operation span\")\n    start_time: float = Field(..., description=\"Epoch timestamp of start of span\")\n    end_time: float = Field(..., description=\"Epoch timestamp of end of span\")\n    project_id: str = Field(default=\"\", description=\"Optional project ID context\")\n    dry_run: bool = Field(default=True, description=\"When True, validates hex ID and returns a mock trace URL\")\n\n\nclass CloudTraceSpanOutput(BaseModel):\n    is_emitted: bool = Field(..., description=\"True if the trace span was successfully processed and dispatched\")\n    trace_url: str = Field(..., description=\"Console URL to view the trace in the browser\")\n    dry_run_executed: bool = Field(..., description=\"True if dry_run logic was utilized\")\n\n\nclass PiCloudTraceSpanEmitter:\n    \"\"\"Dispatches application spans and performance metrics directly into Google Cloud Trace.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiCloudTraceSpanEmitter\"\n\n    def execute(self, input_envelope: CloudTraceSpanInput) -> CloudTraceSpanOutput:\n        \"\"\"Create and emit trace span data to GCP Cloud Trace.\"\"\"\n        trace_id = input_envelope.trace_id.strip()\n        span_name = input_envelope.span_name.strip()\n        start = input_envelope.start_time\n        end = input_envelope.end_time\n        project = input_envelope.project_id.strip() or \"mock-project-id\"\n        dry_run = input_envelope.dry_run\n\n        is_valid_trace = bool(_TRACE_ID_PATTERN.match(trace_id))\n        url = f\"ht
<truncated 1084 bytes>