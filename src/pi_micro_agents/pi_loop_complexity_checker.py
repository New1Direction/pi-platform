"from __future__ import annotations\n\nimport ast\n\nfrom pydantic import BaseModel, Field\n\n\nclass LoopInput(BaseModel):\n    content: str = Field(..., description=\"String contents of python source code\")\n\n\nclass LoopOutput(BaseModel):\n    max_nesting: int = Field(..., description=\"Max nested depth of loop nodes\")\n    exceeds_limit: bool = Field(..., description=\"True if max nesting is above acceptable limit\")\n    status: str = Field(..., description=\"Complexity evaluation status\")\n\n\nclass PiLoopComplexityChecker:\n    \"\"\"Counts maximum nested loop depth and flags excessive complexity.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiLoopComplexityChecker\"\n\n    def check_loop_complexity(self, input_envelope: LoopInput) -> LoopOutput:\n        content = input_envelope.content\n\n        max_nesting = 0\n        limit = 3  # Default loop nesting threshold\n\n        try:\n            tree = ast.parse(content)\n\n            def get_loop_depth(node: ast.AST) -> int:\n                # Returns the maximum nesting depth under this node\n                is_loop = isinstance(node, (ast.For, ast.While, ast.AsyncFor))\n                current_depth = 1 if is_loop else 0\n\n                child_depths = [0]\n                for child in ast.iter_child_nodes(node):\n                    child_depths.append(get_loop_depth(child))\n\n                return current_depth + max(child_depths)\n\n            for node in ast.walk(tree):\n                if isinstance(node, (ast.For, ast.While, ast.AsyncFor)):\n                    # Compute depth starting at this loop root\n                    depth = get_loop_depth(node)\n                    if depth > max_nesting:\n                        max_nesting = depth\n\n        except Exception:\n            # Fallback when parsing fails: read indentation blocks\n            # Look for adjacent indented lines containing \"for \" or \"while \"\n            lines = content.splitlines()\n            nested = 0\n            for line in lines:\n
<truncated 486 bytes>