"from __future__ import annotations\n\nimport logging\nfrom typing import List\n\nfrom pydantic import BaseModel, Field\n\nfrom pi_micro_agents.gcp_client import VERTEX_AVAILABLE\n\nlogger = logging.getLogger(\"pi_gemini_semantic_router\")\n\n\nclass GeminiSemanticRouterInput(BaseModel):\n    goal: str = Field(..., description=\"The orchestrator goal to route to a specific capability\")\n    routes: List[str] = Field(..., description=\"List of potential routes/capabilities to match against\")\n    dry_run: bool = Field(default=True, description=\"When True, routes using a fallback keyword matcher\")\n\n\nclass GeminiSemanticRouterOutput(BaseModel):\n    selected_route: str = Field(..., description=\"The chosen route name from the input list\")\n    confidence_score: float = Field(..., description=\"Routing confidence between 0.0 and 1.0\")\n    reasoning: str = Field(..., description=\"Reasoning or description for the routing choice\")\n    dry_run_executed: bool = Field(..., description=\"True if dry_run logic was utilized\")\n\n\nclass PiGeminiSemanticRouter:\n    \"\"\"Uses Vertex AI Gemini to perform semantic routing of goals to micro-agent capability channels.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiGeminiSemanticRouter\"\n\n    def execute(self, input_envelope: GeminiSemanticRouterInput) -> GeminiSemanticRouterOutput:\n        \"\"\"Route the goal to the most relevant path from a given list of routes.\"\"\"\n        goal = input_envelope.goal.strip()\n        routes = input_envelope.routes\n        dry_run = input_envelope.dry_run\n\n        if not routes:\n            return GeminiSemanticRouterOutput(\n                selected_route=\"\",\n                confidence_score=0.0,\n                reasoning=\"No routes provided to select from.\",\n                dry_run_executed=True,\n            )\n\n        # Keyword matching fallback/dry_run logic\n        def dry_run_route() -> tuple[str, float, str]:\n            goal_words = set(goal.lower().split())\n            best_r
<truncated 2360 bytes>