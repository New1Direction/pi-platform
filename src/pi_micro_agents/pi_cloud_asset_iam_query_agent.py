"from __future__ import annotations\n\nimport logging\nfrom typing import List, Dict, Any\n\nfrom pydantic import BaseModel, Field\n\nfrom pi_micro_agents.gcp_client import ASSET_AVAILABLE\n\nlogger = logging.getLogger(\"pi_cloud_asset_iam_query_agent\")\n\n\nclass CloudAssetQueryInput(BaseModel):\n    scope: str = Field(..., description=\"GCP resource scope (e.g. projects/my-project, folders/1234, organizations/5678)\")\n    query: str = Field(default=\"\", description=\"The IAM query or policy search expression\")\n    dry_run: bool = Field(default=True, description=\"When True, validates scope and returns empty query result list\")\n\n\nclass CloudAssetQueryOutput(BaseModel):\n    query_executed: bool = Field(..., description=\"True if the IAM search request completed successfully\")\n    matched_policies_count: int = Field(..., description=\"Total count of matched IAM policy findings\")\n    matched_policies: List[Dict[str, Any]] = Field(..., description=\"JSON structure list of matched policies\")\n    dry_run_executed: bool = Field(..., description=\"True if dry_run logic was utilized\")\n\n\nclass PiCloudAssetIAMQueryAgent:\n    \"\"\"Queries Cloud Asset Inventory to audit all IAM policies in a scope, detecting shadow bindings and unauthorized access policies.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiCloudAssetIAMQueryAgent\"\n\n    def execute(self, input_envelope: CloudAssetQueryInput) -> CloudAssetQueryOutput:\n        \"\"\"Query IAM policies via Cloud Asset API, validating the query scope.\"\"\"\n        scope = input_envelope.scope.strip()\n        query = input_envelope.query.strip()\n        dry_run = input_envelope.dry_run\n\n        # Scope must start with projects/, folders/, or organizations/\n        is_valid_scope = (\n            scope.startswith(\"projects/\")\n            or scope.startswith(\"folders/\")\n            or scope.startswith(\"organizations/\")\n        )\n\n        if not is_valid_scope:\n            return CloudAssetQueryOutput(\n              
<truncated 1072 bytes>