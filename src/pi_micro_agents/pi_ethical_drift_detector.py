"from __future__ import annotations\n\nfrom typing import List, Dict, Any\nfrom pydantic import BaseModel, Field\n\n\nclass DriftInput(BaseModel):\n    conversation_history: List[str] = Field(..., description=\"Chronological listing of conversational turns\")\n    baseline_ethics: Dict[str, Any] = Field(..., description=\"Ethical bounds, banned word patterns, or guidelines\")\n\n\nclass DriftOutput(BaseModel):\n    drift_detected: bool = Field(..., description=\"Indicates if ethical boundaries were crossed or drifted\")\n    violation_type: str = Field(..., description=\"Identified violation category or list\")\n    status: str = Field(..., description=\"Ethical drift evaluation status\")\n\n\nclass PiEthicalDriftDetector:\n    \"\"\"Tracks subtle ethical drift across multi-turn agent conversations.\"\"\"\n\n    def __init__(self) -> None:\n        self.agent_name = \"PiEthicalDriftDetector\"\n\n    def detect_drift(self, input_envelope: DriftInput) -> DriftOutput:\n        history = input_envelope.conversation_history\n        baseline = input_envelope.baseline_ethics\n\n        # Exposing deterministic banned keyword arrays\n        banned_topics = baseline.get(\"banned_topics\", [])\n        toxic_keywords = baseline.get(\"toxic_keywords\", [\"harm\", \"exploit\", \"bypass\", \"malicious\", \"steal\", \"hack\"])\n\n        drift_detected = False\n        violation_type = \"None\"\n\n        # Scan history for direct banned terms or patterns\n        for turn in history:\n            turn_lower = turn.lower()\n            \n            # Check banned topics\n            for topic in banned_topics:\n                if topic.lower() in turn_lower:\n                    drift_detected = True\n                    violation_type = f\"BANNED_TOPIC_VIOLATION: {topic}\"\n                    break\n\n            # Check general toxic keyword drift\n            for keyword in toxic_keywords:\n                if keyword in turn_lower:\n                    drift_detected = True\n                    violation_type = f\"TOXIC
<truncated 360 bytes>